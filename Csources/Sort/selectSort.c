void selectSort(int *arr, int n)
{
	for(int i=0;i<n-1;i++)
	{
		int index=i;
		for(int j=i+1;j<n;j++)
		{
			if(arr[j]<arr[index])
				index=j;
		}
		
		int tmp=arr[i];arr[i]=arr[index];arr[index]=tmp;
	}
}

void selectSort(int *arr, int n)
{
	for(int i=0;i<n-1;i++)
	{
		int index=i;
		for(int j=i+1;j<n;j++)
		{
			if(arr[j]<arr[index])
				index=j;
		}
		int tmp=arr[i];arr[i]=arr[index];arr[index]=tmp;
	}
}

void selectSort(int *arr, int n)
{
	for(int i=0;i<n-1;i++)
	{
		int index=i;
		for(int j=i+1;j<n;j++)
		{
			if(arr[j]<arr[index])
				index=j;
		}
		int tmp=arr[i];arr[i]=arr[index];arr[index]=tmp;
	}
}


void selectSort(int *arr, int n)
{
	for(int i=0;i<n-1;i++)
	{
		int index=i;
		for(int j=i+1;j<n;j++)
		{
			if(arr[j]<arr[index])
				index=j;
		}
		int tmp=arr[i];arr[i]=arr[index];arr[index]=tmp;
	}
}

void selectSort(int *arr, int n)
{
	for(int i=0;i<n-1;i++)
	{
		int index=i;
		for(j=i+1;j<n;j++)
		{
			if(arr[j]<arr[index])
				index=j;
		}
		int tmp=arr[i];arr[i]=arr[index];arr[index]=tmp;
	}
}
void selectSort(int *arr, int n)
{
	for(int i=0;i<n;i++)
	{
		int index=i;
		for(int j=i+1;i<n;j++)
		{
			if(arr[j]<arr[index])
				index=j;
		}
		int tmp=arr[i];arr[i]=arr[index];arr[index]=tmp;
	}
}



void selectSort(int *arr,int n)
{
	for(int i=0;i<n;i++)
	{
		int index=i;
		for(int j=i+1;i<n;j++)
		{
			if(arr[j]<arr[index])
				index=j;
		}
		int tmp=arr[i];
		arr[i]=arr[index];
		arr[index]=tmp;
	}
}


void selectSort(int *arr,int n)
{
	for(int i=0;i<n;i++)
	{
		int index=i;
		for(int j=i+1;j<n;j++)
		{
			if(arr[j]<arr[index])
				index=j;
		}
		int tmp=arr[i];
		arr[i]=arr[index];
		arr[index]=tmp;
	}
}




























